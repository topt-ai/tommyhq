import { ContactES } from '@/components/es/ContactES';

export default function ContactoPage() {
  return (
    <main className="flex flex-col w-full relative pt-16 min-h-screen">
      <ContactES />
    </main>
  );
}
